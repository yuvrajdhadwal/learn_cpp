int main()
{
    // inline functions allow for multiple definitions of the same function
    // they must be identical

    // historically used to inline functions into the code they were called in
    // this would increase performance since theres no calling overhead but also increases
    // memory footprint of the program
    // modern compilers do this now automatically so no point in doing this anymore

    return 0;
}